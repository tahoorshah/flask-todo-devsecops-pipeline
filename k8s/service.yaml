apiVersion: v1
kind: Service
metadata:
  name: flask-todo-svc
  namespace: production
spec:
  selector:
    app: flask-todo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort
